<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs.b07.cscb07courseproject">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".AdminPanelActivity" />
        <activity android:name=".ConfirmUploadFlightActivity" />
        <activity android:name=".ConfirmUploadClientActivity" />
        <activity android:name=".FlightBookingActivity" />
        <activity android:name=".EditClientInfoActivity" />
        <activity android:name=".EditFlightInfoActivity" />
        <activity android:name=".MasterInputActivity" />
        <activity android:name=".ConfirmUploadAdminActivity" />
        <activity android:name=".CreatePasswordActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".ClientMainMenu" />
        <activity android:name=".ViewBookedItinerariesActivity" />
        <activity android:name=".AdminMainMenu"></activity>
    </application>

</manifest>